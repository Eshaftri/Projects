<div class="div-h1">
  <h1>View Members</h1>
</div>


<table class="members_table" width="100%" cellspacing="0">
  <thead>
    <tr>
      <th>Title</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Address</th>
      <th>DoB</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @members.each do |member| %>
    <tr>
      <td><%= member.title %></td>
      <td><%= member.first_name %></td>
      <td><%= member.last_name %></td>
      <td><%= member.address%></td>
      <td><%= member.dob %></td>
      <td>
        <div class="btn-group">
          <form  class="form-del" action="/members/<%= member.id %>/delete" method="POST">
            <a href="/attendants" class="btn btn-view">Select Class</a>
            <a href="/members/<%=member.id%>/edit"   class="btn btn-edit">Edit</a>
            <button type="submit" class="btn btn-delete">Delete</button>
          </form>
        </div>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
